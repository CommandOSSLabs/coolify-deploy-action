name: Act Test

on:
  workflow_dispatch:

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Create Coolify service
        id: deploy
        uses: ./
        with:
          coolify_domain: ${{ env.COOLIFY_DOMAIN }}
          api_token: ${{ secrets.COOLIFY_API_TOKEN }}
          docker_compose: test/compose.yml
          project_uuid: ${{ env.TEST_PROJECT_UUID }}
          server_uuid: ${{ env.TEST_SERVER_UUID }}
          environment_name_or_uuid: ${{ env.TEST_ENVIRONMENT_UUID }}
          service_options: |
            {
              "name": "web",
              "description": "Act test service"
            }
          environment_variables: |
            [
              { "key": "NODE_ENV", "value": "test", "is_secret": false },
              { "key": "API_TOKEN", "value": "secret-value", "is_secret": true }
            ]

  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Update Coolify service
        id: deploy
        uses: ./
        with:
          coolify_domain: ${{ env.COOLIFY_DOMAIN }}
          api_token: ${{ secrets.COOLIFY_API_TOKEN }}
          docker_compose: test/compose.yml
          service_uuid: ${{ env.TEST_SERVICE_UUID }}
          project_uuid: ${{ env.TEST_PROJECT_UUID }}
          server_uuid: ${{ env.TEST_SERVER_UUID }}
          environment_name_or_uuid: ${{ env.TEST_ENVIRONMENT_UUID }}
          service_options: |
            {
              "name": "web",
              "description": "Updated via act",
              "urls": [
                { "name": "web", "url": "https://example.test" }
              ]
            }
